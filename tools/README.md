# Tools

Optional local helper scripts. The ChatGPT runtime does not require these files.

- `validate_charter.py` performs a lightweight structural check against expected charter sections.
- `generate_charter.py` is a simple sample generator retained for local experimentation.
